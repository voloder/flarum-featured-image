export default class FeaturedImageButton {
    view() {
        return (
            <button className="Button" type="button" name="add-featured-image">
                <span className="Button-label">Choose an Image...</span>
            </button>
        );
    }
}